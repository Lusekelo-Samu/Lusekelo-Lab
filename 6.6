#include < iostream>
#include < math.h>

using namespace std;

int main()
{
	int a, result, value;

	cout << "enter Number a:" << endl;
	cin >> a;

	value = pow(a, 2); // a squared = a ^ 2

		result = value * value * value * value;
	// a^8 = a^2 * a^2 * a^2 * a^2

	cout  << "the result is" << result << "for a =" << a << endl;
}
