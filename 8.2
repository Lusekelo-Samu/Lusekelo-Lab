#include < iostream>
using namespace std;

int main()
{
	int a, b;
	do {
		cout << "a must be greater than a B" << endl;
		cout << "enter a" << endl;
		cin >> a;
		cout << "enter B" << endl;
		cin >> b;
	} while (a < b);
	cout << "the maximum number of segments of B over A is" << a / b; // integer division
	// we take the integer part of the number a comma found

	return 0;
}
