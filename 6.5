# include <iostream> // note the syntax of The includes directives
#include < math.h>

using namespace std;

int main() {

	int x, result;
	cout << "enter number x:" << endl;
	cin >> x;

	result = 4 * (pow(x - 3, 6)) - 6 * (pow(x - 3, 3)) + 2;
	cout << "the result is" << result << "for x =" << x << endl;
}
