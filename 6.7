# include < iostream>
# include < math.h>

using namespace std;

int main()
{
	int a, result, value;

	cout << "enter Number a:" << endl;
	cin >> a;

	value = pow(a, 3); // a girl to the cube = a ^ 3

		result = value * value * value * value * value;
	 // a^15 = a^3 * a^3 * a^3 * a^3 * a^3;

	cout << "the result is "<< result << "for a = " << a << endl;
}
