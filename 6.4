#include < iostream>
#include < math.h>

using namespace std;

int main()
{
	int x, result;

	cout << "enter number x:" << endl;
	cin >> x;

	result = 3 * (pow(x, 6)) - 6 * (pow(x, 2)) - 7;
	cout << "the result is" << result << "for x =" << x << endl;
}
