#include < iostream>
#include < math.h>
using namespace std;

int main() 
{
	int nOctets;
	cout << "enter size in bytes" << endl;
	cin >> nOctets;
	cout << nOctets << "bytes =" << nOctets / pow(10, 3) << " kilo bytes";
	return 0;
}
